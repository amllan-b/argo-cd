apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: alloy
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default
  sources:
    # Helm chart source
    - repoURL: https://grafana.github.io/helm-charts
      chart: alloy
      targetRevision: "1.2.1"  # specify the version you want
      helm:
        valueFiles:
          - $values/alloy/values.yaml
    
    - repoURL: https://github.com/amllan-b/argo-cd.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: monitoring
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
